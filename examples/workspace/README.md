# SillPak sample workspace

This directory exists so the routed shell can be exercised without granting it a personal folder.

- Open `notes.md` to test formatted Markdown and Lexical editing.
- Open `data.csv` to test the spreadsheet projection.
- Open `sample.ts` to test CodeMirror.
- Start an agent CLI in the persistent terminal and let route navigation remain independent.
