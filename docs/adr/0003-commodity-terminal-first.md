# ADR 0003: Commodity terminal first

**Status:** accepted

The shipping stack uses xterm.js and node-pty. A custom Rust PTY broker, Wasm terminal state engine, and renderer are earned only by measured product pain. Terminal correctness is not the first differentiated layer.
