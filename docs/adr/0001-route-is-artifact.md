# ADR 0001: Route is artifact identity

**Status:** accepted

The active filesystem artifact is represented by the Astro route. Directory browsing and preview are one routed page model. A second selected-file store is rejected because it can drift from browser history and breadcrumbs.
